import { FunctionComponent, ReactNode } from 'react';

// Style
import * as Style from './style';

const Gallery: FunctionComponent = (): ReactNode => {
	return <Style.Container>Gallery</Style.Container>;
};

export default Gallery;
