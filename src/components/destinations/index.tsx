import { FunctionComponent, ReactNode } from 'react';

// Style
import * as Style from './style';

const Destinations: FunctionComponent = (): ReactNode => {
	return <Style.Container>Destinations</Style.Container>;
};

export default Destinations;
